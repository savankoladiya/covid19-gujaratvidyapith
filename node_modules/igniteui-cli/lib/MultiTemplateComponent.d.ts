export declare abstract class MultiTemplateComponent<T extends Template> implements Component {
    private testType;
    private templatesMap;
    name: string;
    group: string;
    description: string;
    groupPriority: number;
    readonly templates: T[];
    /**
     *
     */
    constructor(testType: any, templatesMap: Map<string, {
        name: string;
        widget: string;
    }>);
    private create;
    /**
     * Create a Template for a specific editor
     * @param template Template id/type to use from mapping, e.g. "text-editor"
     */
    private makeTemplate;
    private getTemplate;
}
